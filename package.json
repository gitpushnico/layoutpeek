{
  "name": "layoutpeek",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "release": "vite build && node scripts/gen-bookmarklet.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "terser": "^5.46.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
